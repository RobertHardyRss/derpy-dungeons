export class SwitchStates {
	/**
	 * @param {string} off - the image frame for the off state of the switch
	 * @param {string} on  - the image frame for the on state of the switch
	 */
	constructor(off, on) {
		this.off = off;
		this.on = on;
	}
}
